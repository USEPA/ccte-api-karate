<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">

    <!-- Console Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Keep Karate internal engine logging at DEBUG so it captures data for reports -->
    <logger name="com.intuit.karate" level="DEBUG"/>
    <logger name="io.karatelabs" level="DEBUG"/>
    <logger name="karate" level="DEBUG"/>
    
    <!-- Mute noisy third-party frameworks -->
    <logger name="org.thymeleaf" level="ERROR"/>
    <logger name="ch.qos.logback" level="WARN"/>
    <logger name="org.apache" level="WARN"/>

    <!-- CRITICAL: Set root console logging to INFO or WARN to clean up terminal noise -->
    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>

</configuration>
